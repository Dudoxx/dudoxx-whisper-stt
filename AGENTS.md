# AGENTS.md - ddx-ai-whisper

## Repo Policy

- Default branch: `main`
- This repository is used as a submodule of `dudoxx-hapifihr` (see `../.gitmodules`)

## Quick Commands

```bash
python3 -m venv venv
source venv/bin/activate
pip install -e .
# start command depends on the chosen runner; see INSTALL.md
```
