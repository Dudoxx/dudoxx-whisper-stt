# Voxtral Mini 3B Dependencies
# License: Apache 2.0

# =============================================================================
# Core Framework
# =============================================================================
vllm>=0.8.0                    # LLM serving with audio support
torch>=2.0.0                   # PyTorch (CUDA enabled)
fastapi>=0.100.0               # Web framework
uvicorn>=0.23.0                # ASGI server
httpx>=0.24.0                  # Async HTTP client

# =============================================================================
# Audio Processing
# =============================================================================
numpy>=1.24.0                  # Numerical computing
scipy>=1.10.0                  # Signal processing (optional)

# =============================================================================
# Voice Activity Detection (VAD)
# =============================================================================
silero-vad>=5.0.0              # Neural VAD (ONNX)
onnxruntime>=1.15.0            # ONNX runtime

# =============================================================================
# Speaker Diarization
# =============================================================================
pyannote.audio>=3.1.0          # Speaker diarization pipeline
# Note: Requires HuggingFace token with pyannote access
# Request access at: https://huggingface.co/pyannote/speaker-diarization-3.1

# =============================================================================
# Text Processing
# =============================================================================
# Punctuation Restoration
transformers>=4.35.0           # HuggingFace transformers
deepmultilingualpunctuation    # Fallback punctuation model

# Named Entity Recognition
gliner>=0.2.0                  # Zero-shot NER

# Semantic Paragraph Detection
sentence-transformers>=2.2.0   # Sentence embeddings

# =============================================================================
# Utilities
# =============================================================================
python-multipart>=0.0.6        # File uploads in FastAPI

# =============================================================================
# Installation Notes
# =============================================================================
# 1. Create virtual environment:
#    python3.11 -m venv venv
#    source venv/bin/activate
#
# 2. Install PyTorch with CUDA first:
#    pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# 3. Install requirements:
#    pip install -r requirements.txt
#
# 4. Accept pyannote license at HuggingFace:
#    https://huggingface.co/pyannote/speaker-diarization-3.1
#
# 5. Set HF_TOKEN in .env file
